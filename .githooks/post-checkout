#!/bin/sh

repo_root=$(git rev-parse --show-toplevel)
cd "$repo_root" || exit 0

python3 scripts/setup/bootstrap_local_state.py --quiet || {
    echo "warning: could not initialize local SLM lab workspace" >&2
    exit 0
}
