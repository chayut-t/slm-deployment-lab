# Workflows

Portable CI, documentation, and manually dispatched Qualcomm workflows belong
here. Hardware or paid-service workflows must never run implicitly on pull
requests and must follow the secret policy in `AGENTS.md`.
