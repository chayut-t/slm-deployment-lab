# Reproducible environments

Environment-specific setup belongs in:

- `macos-m4/`
- `linux-cuda/`
- `linux-aimet/`

Pin versions when reproducibility requires it and record externally installed
SDK/runtime versions in host manifests.
