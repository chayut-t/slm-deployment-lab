# Tests

Tests will cover graph/cache contracts, export parity, backend behavior,
manifest schemas, numerical tolerances, and automation. Hardware-dependent
tests must identify their target and skip with a precise reason when the
required environment is unavailable.
