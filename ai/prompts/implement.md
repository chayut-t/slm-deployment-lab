# Implementation task prompt

Implement task `TNN` according to:

- `AGENTS.md`
- `docs/project/plan.md`
- `ai/tasks/task_graph.yaml`
- `ai/plans/active/TNN-short-slug.md` when present

Confirm that dependencies are complete, preserve unrelated work, implement only
the assigned scope, run relevant verification, update public artifacts, and
write the required worklog and handoff. Report exact blockers instead of
silently changing the model, target, precision, or benchmark contract.
