{
  "id": "T72",
  "title": "Manual GitHub Actions AI Hub workflow",
  "depends_on": ["T03", "T30"],
  "resource_locks": [],
  "objective": "Teach and automate manually dispatched Qualcomm jobs after the local path is stable.",
  "owned_paths": [".github/workflows/qualcomm-benchmark.yml", ".github/workflows/qualcomm-request-bundle.yml", "docs/learning/github_actions_for_ai_hub.md", "tests/workflows/"],
  "outputs": ["workflow_dispatch Qualcomm workflow", "Reviewed request-bundle producer workflow", "Actions learning guide", "Workflow validation tests"],
  "acceptance": ["Secrets are never exposed to untrusted forks", "Inputs select target, context, precision, and stage", "Local scripts remain the implementation source", "Bundle provenance binds a fixed producer workflow, exact revision/run, same-repository release tag/asset, and reviewed source digest", "Producer extraction rejects unsafe paths and emits a complete content-addressed inventory plus reviewed bundle manifest", "Before secret configuration the benchmark validates canonical paths, full target selector, precision metadata, exact T12 context contract, predecessor lineage, and every request/artifact digest"]
}
