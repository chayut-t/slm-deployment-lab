{
  "id": "T23",
  "title": "Prefill re-export promotion and evidence refresh",
  "depends_on": ["T20", "T21"],
  "resource_locks": ["t9_heavy_io"],
  "objective": "Promote the Concat prefill cache write into the attested reference export, and bring every committed record that describes those graphs back into agreement with the bytes on disk.",
  "owned_paths": ["src/slm_lab/export/onnx_matrix.py", "results/manifests/onnx/", "results/graph/", "docs/results/onnx/"],
  "outputs": ["Re-exported and re-attested reference prefill graphs with regenerated manifests", "Refreshed graph-inspection and ORT CPU parity evidence measured at ORT_DISABLE_ALL", "A committed audit tool that enumerates every numeric claim about the reference graphs", "A confirmed or replaced ORT CPU tolerance with its derivation recorded"],
  "acceptance": ["All four prefill graphs load on the ONNX Runtime CPU provider at ORT_DISABLE_ALL", "Decode graph digests and the shared external-data digest are unchanged by the re-export", "Every committed record naming a reference graph digest, size, node count, or operator count agrees with the re-exported artifacts, verified by the audit tool rather than by inspection", "The export attestation is regenerated by its own tooling and no recorded digest is hand-edited", "The ORT CPU tolerance no longer reads proposed_unvalidated, whether it was confirmed or replaced, and a parity failure is recorded as a failure rather than absorbed by widening a threshold"]
}
