# Learning notebooks

Notebooks combine concise engineering context, a runnable baseline, editable
experiments, plots, and interpretation prompts. They should import reusable
logic from `src/slm_lab/` rather than duplicating production implementations.

Large outputs and caches belong under `artifacts/`; commit only small evidence
that materially improves the educational experience.
