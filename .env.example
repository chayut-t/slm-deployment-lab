# Large artifacts stay outside Git.
SLM_LAB_ARTIFACT_ROOT=/Volumes/T9/slm-deployment-lab

# Keep common model/download caches on the external SSD when available.
HF_HOME=/Volumes/T9/slm-deployment-lab/hf-cache

# Add credentials only in an ignored .env or approved secret store.
# QAI_HUB_API_TOKEN=
