# Repository scripts

- `ai/`: task status and worklog lifecycle tools.
- `repo/`: repository validation and hygiene tools.
- `setup/`: local workspace and Git-hook setup.

Prefer small, idempotent scripts with clear inputs and nonzero exits on
validation failure.
